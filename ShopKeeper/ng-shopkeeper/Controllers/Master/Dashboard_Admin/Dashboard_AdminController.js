"use strict";

define(['application-configuration'], function (app)
{

    app.register.directive('ngDashboard', function ($compile)
    {
       
    });

    app.register.controller('dashboardController', ['$scope', '$rootScope', '$routeParams',
    function ($scope, $rootScope, $routeParams, storeDepartmentServices, alertsService)
    {
        
    }]);

});