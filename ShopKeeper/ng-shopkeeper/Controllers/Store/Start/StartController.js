"use strict";

define(['application-configuration'], function (app)
{
    app.register.controller('startController', ['$scope', '$rootScope',
    function ($scope, $rootScope)
    {
       

    }]);

});


